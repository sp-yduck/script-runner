name: example pipeline
tasks:
- name: kubectl
  command:
  - which kubectl
  - kubectl version

- name: helm
  command:
  - which helm
  - helm version

- name: fail task
  command:
  - kubectl get node